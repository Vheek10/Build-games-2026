{
	"name": "stratadeed-contracts",
	"version": "1.0.0",
	"description": "StrataDeed Solidity smart contracts for Avalanche C-Chain",
	"scripts": {
		"compile": "hardhat compile",
		"test": "hardhat test",
		"deploy:fuji": "hardhat run scripts/deploy.js --network fuji",
		"deploy:mainnet": "hardhat run scripts/deploy.js --network avalanche",
		"deploy:local": "hardhat run scripts/deploy.js --network hardhat",
		"node": "hardhat node",
		"clean": "hardhat clean"
	},
	"devDependencies": {
		"@nomicfoundation/hardhat-toolbox": "^4.0.0",
		"@openzeppelin/contracts": "^5.1.0",
		"dotenv": "^16.4.0",
		"hardhat": "^2.22.0"
	}
}
