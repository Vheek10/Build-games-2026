/** @format */

// Mantle/viem clients removed for Sui-only mode. Export minimal placeholders.
export const mantlePublicClient = undefined;
export const mantleSepoliaPublicClient = undefined;
export const getPublicClient = (_chainId?: number) => undefined;
